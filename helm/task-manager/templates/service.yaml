apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-task-manager-service
spec:
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
